const dice = {
  sides: 6,
  roll: function () {
    return Math.floor(Math.random() * this.sides) + 1;
  }
}
const numberofdice = prompt('How many dices you want to roll?')
const sum = prompt('Tell me the target sum to calculate probability')

const button = document.getElementById('button');

let n = 0

button.onclick = while(n !== 10000){

}